# إعداد نظام رفع الصور M&F Wallpapers

## المتطلبات
- تكامل Supabase مُفعّل
- قاعدة بيانات Supabase
- Storage bucket للملفات

## خطوات الإعداد

### 1. تفعيل Supabase
1. اضغط على الزر الأخضر "Supabase" في أعلى يمين الواجهة
2. اتبع خطوات الاتصال بـ Supabase
3. تأكد من إكمال الإعداد بالكامل

### 2. إعداد قاعدة البيانات
قم بتشغيل الملف `supabase_setup.sql` في SQL Editor في لوحة تحكم Supabase:

```sql
-- سيتم إنشاء:
- جدول media_files لحفظ معلومات الملفات
- Storage bucket للملفات
- Row Level Security policies
- وظائف للتحديث التلقائي
```

### 3. اختبار النظام
1. انتقل إلى `/admin`
2. أدخل كلمة المرور: `M&F | 11:11 |`
3. انتقل إلى صفحة الرفع
4. قم برفع الملفات (صور أو ملفات صوتية)

## الميزات

### صفحة الإدارة (`/admin`)
- حماية بكلمة مرور
- واجهة تسجيل دخول أنيقة
- تشفير كلمة المرور

### صفحة الرفع (`/upload`)
- رفع ملفات متعددة
- دعم الصور والملفات الصوتية
- إضافة العنوان والوصف والفئات والعلامات
- عرض المعاينة للملفات المختارة
- رسائل حالة واضحة

### الأمان
- جميع الملفات محفوظة في Supabase Storage
- Row Level Security مُفعّل
- معالجة أخطاء شاملة
- نسخ احتياطية تلقائية

## استكشاف الأخطاء

### خطأ "Missing Supabase environment variables"
- تأكد من تفعيل تكامل Supabase بشكل صحيح
- انتظر دقائق قليلة بعد التفعيل
- تأكد من وجود المتغيرات VITE_SUPABASE_URL و VITE_SUPABASE_ANON_KEY

### خطأ في رفع الملفات
- تأكد من إنشاء storage bucket بالاسم 'media'
- تأكد من تطبيق storage policies
- تحقق من console logs للمزيد من التفاصيل

## الاستخدام

### للمطورين
```javascript
// استيراد Supabase client
import { supabase } from "@/integrations/supabase/client";

// رفع ملف
const { error } = await supabase.storage
  .from('media')
  .upload(filePath, file);

// حفظ المعلومات في قاعدة البيانات
const { error } = await supabase
  .from('media_files')
  .insert(metadata);
```

### للمستخدمين
1. اضغط على زر الإعدادات ⚙️ في الهيدر
2. أدخل كلمة المرور
3. ارفع الملفات المطلوبة
4. أضف المعلومات التوضيحية
5. اضغط رفع

## تخصيصات إضافية
- يمكن تعديل أنواع الملفات المدعومة
- يمكن إضافة المزيد من الحقول للبيانات الوصفية
- يمكن تخصيص أحجام الملفات المسموحة
- يمكن إضافة معاينة للملفات